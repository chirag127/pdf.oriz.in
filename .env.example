# OrizPDF Environment Variables
# No secrets required for core PDF tools (all client-side)
# Copy to .env and fill in as needed

# Puter.js AI (optional - handled by client-side Puter auth)
# No API keys needed - Puter.js uses user's own account
